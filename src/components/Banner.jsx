import styles from "../module/Banner.module.css";

const Banner = () => {
  return (
    <div className={styles.infoTextContainer}>
      <p>No order minimum | Exclusive pricing | Dedicated support</p>
    </div>
  );
};

export default Banner;
